const bottrs = document, querySelectronALL('bottrs')

bottors.forEach(botao => {
    botao.addEventListener("click",  curtir)

    function curtir() {
        let curtidas  = document, querySelectronALL("spam")
        curtidas, textContext+
    }
});